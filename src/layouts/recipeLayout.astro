---
import MainLayout from "./mainLayout.astro";

const { frontmatter } = Astro.props;
---

<MainLayout
  title={frontmatter.title}
  description={frontmatter.description}
  OpenGraphImg={frontmatter.image}
>
  <div><slot /></div>
</MainLayout>
